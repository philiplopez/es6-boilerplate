{
  "name": "",
  "version": "0.0.1",
  "description": "TODO PACKAGE NAME & DESCRIPTION",
  "main": "build/index.js",
  "dependencies": {
  },
  "devDependencies": {
    "babel": "^5.6.4",
    "babel-core": "^5.6.2",
    "babel-loader": "^5.1.4",
    "chai": "^3.0.0",
    "eslint": "^0.20.0",
    "mocha": "^2.2.5"
  },
  "scripts": {
    "test": "mocha --compilers js:babel/register --recursive --ui bdd --reporter spec",
    "build": "babel src/js --out-dir build",
    "clean": "rm -rf build",
    "lint": "eslint src/js"
  },
  "author": "TODO",
  "license": "TODO"
}
